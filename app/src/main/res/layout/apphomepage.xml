<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context="com.example.sathwika.MainActivity">

    <!-- 2/3 Page Background Image -->
    <TextView
        android:id="@+id/tvGreeting"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/home_page_gm_text"
        android:textSize="22sp"
        android:textStyle="normal"
        android:textColor="#444444"
        android:layout_alignParentStart="true"
        android:layout_marginTop="20dp"
        android:layout_marginStart="20dp"
        android:gravity="start"
        android:elevation="3dp"/>

    <TextView
        android:id="@+id/tvWelcome"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/home_page_heading"
        android:textSize="30sp"
        android:textStyle="bold"
        android:textColor="#000000"
        android:layout_alignParentStart="true"
        android:layout_below="@id/tvGreeting"
        android:layout_marginTop="5dp"
        android:layout_marginStart="20dp"
        android:gravity="start"
        android:elevation="5dp"/>

    <ImageView
        android:id="@+id/homeImage"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:layout_below="@id/tvWelcome"
        android:layout_alignParentTop="true"
        android:layout_above="@id/bottomNavBar"
        android:scaleType="centerCrop"
        android:src="@drawable/home_page_bg"
        android:contentDescription="@string/app_homepage_content_des" />

    <!-- Get Started Button (Bottom-Right) -->

    <!-- Bottom Navigation Bar -->

    <Button
        android:id="@+id/btnGetStarted"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="90dp"
        android:backgroundTint="?attr/colorPrimary"
        android:padding="12dp"
        android:text="@string/app_homepage_get_started"
        android:textColor="?android:attr/textColorPrimaryInverse"
        android:textSize="18sp"
        android:textStyle="bold"
        android:elevation="5dp"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_above="@id/bottomNavBar"
        android:background="#CCCCCC" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@color/white"
        app:itemIconTint="@color/black"
        app:itemTextColor="@color/black"
        app:menu="@menu/homepage_bottom_nav_menu"
        app:labelVisibilityMode="labeled"
        android:elevation="10dp"
        app:layout_behavior="androidx.coordinatorlayout.widget.CoordinatorLayout$Behavior"
        tools:ignore="UsingBottomNavigationView" />

</RelativeLayout>
